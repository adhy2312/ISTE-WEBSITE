# ── Core HTTP / scraping ─────────────────────────────────────────────────────
requests==2.32.3
httpx==0.27.0
beautifulsoup4==4.12.3
fake-useragent==1.5.1
python-dotenv==1.0.1

# ── Browser automation ───────────────────────────────────────────────────────
# 1.44.0 has a Chromium binary hash mismatch on Ubuntu 24.04 (GitHub Actions runner).
# Upgrading to 1.50.0 resolves the playwright install --with-deps failure.
playwright==1.50.0
playwright-stealth==1.0.6

# ── ML / NLP ─────────────────────────────────────────────────────────────────
torch==2.3.1
# huggingface_hub is pinned here to prevent pip from thrashing between the version
# required by sentence-transformers>=0.15.1 and transformers==4.41.2>=0.21.0.
# 0.23.4 satisfies both constraints without any breakage.
huggingface_hub==0.23.4
sentence-transformers==3.0.1
transformers==4.41.2
tokenizers==0.19.1
numpy==1.26.3
pandas==2.2.2

# ── Vector search ────────────────────────────────────────────────────────────
faiss-cpu==1.8.0

# ── Job scraping ─────────────────────────────────────────────────────────────
python-jobspy==1.1.80

# ── Database (async SQLAlchemy) ───────────────────────────────────────────────
sqlalchemy==2.0.30
aiosqlite==0.20.0

# ── Schema / validation ───────────────────────────────────────────────────────
pydantic==2.7.4
PyJWT==2.8.0

# ── Scheduling (optional — for autonomous/APScheduler mode) ──────────────────
apscheduler==3.10.4

# ── Networking / graph ────────────────────────────────────────────────────────
networkx==3.3

# ── Resilience ───────────────────────────────────────────────────────────────
pybreaker==1.2.0

# ── Terminal / monitoring ─────────────────────────────────────────────────────
rich==13.7.1

# ── Celery worker (optional — only needed for local worker mode) ──────────────
# celery==5.4.0
# redis==5.0.4
# flower==2.0.1

# ── API server (optional — not used in CI pipeline) ──────────────────────────
# fastapi==0.111.0
# uvicorn==0.30.1
# slowapi==0.1.9
# sentry-sdk==2.5.1
# prometheus-client==0.20.0
